node_modules
.git
.dockerignore
Dockerfile
docker-compose.yml

# Explanation:

# node_modules: Excludes your local node_modules folder (we’ll use the one inside the container).

# .git: Excludes Git-related files.

# .dockerignore and Dockerfile: Excludes these files to avoid unnecessary copying.

# docker-compose.yml: Excludes the Compose file (it’s not needed in the image).